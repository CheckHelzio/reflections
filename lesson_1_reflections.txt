How did viewing a diff between two versions of a file help you see the bug that
was introduced?

    Porque de esa manera podemos verificar los cambios que realizamos e iden
    tificar los posibles errores que se cometieron

How could having easy access to the entire history of a file make you a more
efficient programmer in the long term?

	1.- No perdemos tiempo al realizar copias de las versiones anteriores
	manualmente.

	2.- Tenemos al alcance todos los cambios realizados.

What do you think are the pros and cons of manually choosing when to create a commit,
like you do in Git, vs having versions automatically saved, like Google Docs does?

	El problema en que se guarden automaticamente es que podemos tener demasiadas
	versiones de un archivos que no tengas cambios significantes y sea dificil lo
	calizar los cambios entre uno y otro

	Por otro lado el guardarlos manualmente puede ser un problema al no saber que
	tan seguido se deberia guardar una nueva version y tener muchas versiones con
	cambios menores, o pocas versiones con grandes cambios.

Why do you think some version control systems, like Git, allow saving multiple files
in one commit, while others, like Google Docs, treat each file separately?

	Esto es debido a que en ciertos casos tiene más logica hacer los cambios de manera
	separa, o no hay necesidad de hacer cambios en archivos si estos no estan relacionados
	entre si.

	En el caso de los archivos en programacion, por lo regular estos se relacionan con
	algun otro archivo. Por lo cual es de gran utilidad que cada vez que se hace un cambio
	se guarde todo el repositorio.

How can you use the commands git log and git diff to view the history of files?

	Primero descargamos el repositorio de GIT y despues vamos a la carpeta del repositorio
	desde la terminal.

	Una vez en la carpeta podemos ingresar el comando "git log" para ver el historial de cambios
	y obtener la lista de los id por cada uno, con los id podemos ver la diferencia entre en un
	cambio y otro con el comnado "git diff VERSION_ANTERIOR VERSION_NUEVA"

How might using version control make you more confident to make changes that could break something?
	
	Porque podemos regresar en cualquier momento y no preocuparnos de que los cambios sean irreparables

